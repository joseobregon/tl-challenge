import styled from 'styled-components'
import { LIGHT_GREY } from '../../constants/colors'

export const SideCartContentStyled = styled.div`
    height: calc(100vh - 155px);
    padding: 12px;
    border-top: 1px solid ${LIGHT_GREY};
    overflow: scroll;
`
